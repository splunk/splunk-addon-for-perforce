[monitor:///opt/perforce/servers/.../logs/(audit|errors|events|auth|track).csv]
disabled = true
index = helix
sourcetype = hx:csv

# TODO remove the following as soon as its content is known
[monitor:///opt/perforce/servers/.../logs/integrity.csv]
disabled = true
index = helix
sourcetype = hx:integrity

# TODO remove the following as soon as its content is known
[monitor:///opt/perforce/servers/.../logs/route.csv]
disabled = true
index = helix
sourcetype = hx:route

# TODO remove the following as soon as its content is known
[monitor:///opt/perforce/servers/.../logs/triggers.csv]
disabled = true
index = helix
sourcetype = hx:triggers

# TODO remove the following as soon as its content is known
[monitor:///opt/perforce/servers/.../logs/user.csv]
disabled = true
index = helix
sourcetype = hx:user